Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Add Vitest for shared and server tests.

Changed:
- Added Vitest as a root workspace dev dependency so shared and server tests use the same runner version.
- Updated the shared package test script to run Vitest, then TypeScript checking.
- Updated the server package test script to run Vitest, then TypeScript checking.
- Marked the matching PRD task complete.

Key decisions:
- Kept actual test coverage for the separate unchecked PRD task that asks tests to cover shared constants/helpers, acknowledgement helpers, Join Code collision retry, and activity service construction.
- Used `--passWithNoTests` because this iteration adds the test runner foundation before test files exist.
- Preserved `tsc --noEmit` in package test scripts so the existing type-check feedback loop remains part of `npm test`.

Blockers:
- None.

Files changed:
- `package.json`
- `package-lock.json`
- `shared/package.json`
- `server/package.json`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm test -w @frempower/shared`
- Passed: `npm test -w @frempower/server`

Notes for next iteration:
- A good next task is to create the in-memory activity service/store skeleton separate from Socket.IO handlers, then add focused construction and dependency-injection tests once the skeleton exists.
