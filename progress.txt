Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Activity service does not import or directly use Socket.IO.

Changed:
- Added a focused server architecture test that reads the activity service source and fails if it imports Socket.IO.
- Marked the matching PRD task complete.

Key decisions:
- Kept the guardrail in the server test suite because the boundary belongs to server architecture, not shared contracts.
- Checked only direct Socket.IO imports in the activity service so the test enforces the transport boundary without overreaching into general dependency policy.

Blockers:
- None.

Files changed:
- `server/src/activity/activityService.test.ts`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm test -w @frempower/server`

Notes for next iteration:
- A good next task is to complete the broader shared/server test coverage task, because Join Code collision retry already has coverage and the remaining gap is shared constants/helpers and acknowledgement result helpers.
