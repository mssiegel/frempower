Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Shared package exports a standard command acknowledgement result type with success/error discriminated union.

Changed:
- Added `CommandAcknowledgementSuccess`, `CommandAcknowledgementError`, and `CommandAcknowledgementResult` exports to `@frempower/shared`.
- Added small `commandAcknowledgementSuccess` and `commandAcknowledgementError` helpers so client and server code can construct the standard acknowledgement shape consistently.
- Marked the matching PRD task complete.

Key decisions:
- Used an `ok: true` / `ok: false` discriminant because Socket.IO command acknowledgements need a compact success/error result shape.
- Kept the error payload generic in this slice so the stable machine-readable error code contract remains a separate unchecked task.
- Did not add Vitest, Socket.IO wiring, authentication, database, Docker, or product flows.

Blockers:
- None.

Files changed:
- `shared/src/index.ts`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm run build -w @frempower/shared`

Notes for next iteration:
- A good next task is to define stable command acknowledgement error codes separate from user-facing copy.
