Current Codebase Socket.IO implementation progress

Current mode:
- Ralph remains human-in-the-loop.
- `ralph-once.sh` works through local issue files under `local/issues/` in numeric order.
- Each Ralph iteration implements exactly one unchecked acceptance criterion from the selected local issue.
- Iteration diffs are left uncommitted for software engineering author review.

Current local issue:
- `local/issues/005-add-realtime-boundary-regression-tests.md` is in progress.

Completed local issues:
- `local/issues/001-configure-realtime-heartbeat-baseline.md`
- `local/issues/002-add-session-room-routing-foundation.md`
- `local/issues/003-emit-realtime-updates-through-rooms.md`
- `local/issues/004-recover-authoritative-state-after-reconnect.md`

Completed criteria:
- `local/issues/001-configure-realtime-heartbeat-baseline.md`: The Realtime Server configures explicit Socket.IO `pingInterval` and `pingTimeout` values.
- `local/issues/001-configure-realtime-heartbeat-baseline.md`: The chosen heartbeat values are shorter than the existing domain reconnect grace periods.
- `local/issues/001-configure-realtime-heartbeat-baseline.md`: Server disconnect logging or instrumentation clearly treats Socket.IO disconnect as the transport-level signal for future **Teacher Disconnect** and **Student Disconnect** behavior.
- `local/issues/001-configure-realtime-heartbeat-baseline.md`: The Client connection hook reflects connected and disconnected states from Socket.IO events.
- `local/issues/001-configure-realtime-heartbeat-baseline.md`: Tests or a small verification path cover connection and disconnect state changes.
- `local/issues/002-add-session-room-routing-foundation.md`: The Realtime Server has a connection registry that tracks `Session ID -> current socket ID`.
- `local/issues/002-add-session-room-routing-foundation.md`: The registry enforces only one live socket connection per **Session ID**.
- `local/issues/002-add-session-room-routing-foundation.md`: If a new socket resumes the same **Session ID**, the registry replaces the old socket with the new current socket.
- `local/issues/002-add-session-room-routing-foundation.md`: A participant is considered connected while the current socket remains registered for the **Session ID**.
- `local/issues/002-add-session-room-routing-foundation.md`: The registry removes the current socket on disconnect and reports when the **Session ID** has no current socket.
- `local/issues/002-add-session-room-routing-foundation.md`: Server-private room naming helpers exist for **Session ID** rooms, teacher activity rooms, and active **Pairing** rooms.
- `local/issues/002-add-session-room-routing-foundation.md`: Tests cover single-live-socket replacement, current-socket disconnect, and room name stability.
- `local/issues/003-emit-realtime-updates-through-rooms.md`: The Realtime Server exposes or uses helpers for emitting to the teacher activity room.
- `local/issues/003-emit-realtime-updates-through-rooms.md`: The Realtime Server exposes or uses helpers for emitting to the current socket in a student **Session ID** room.
- `local/issues/003-emit-realtime-updates-through-rooms.md`: The Realtime Server exposes or uses helpers for emitting to an active **Pairing** room.
- `local/issues/003-emit-realtime-updates-through-rooms.md`: Room delivery assumes only one live socket per **Session ID**.
- `local/issues/003-emit-realtime-updates-through-rooms.md`: Tests prove that a message targeted to one room is not delivered to sockets outside that room.
- `local/issues/003-emit-realtime-updates-through-rooms.md`: No new realtime path uses global broadcast plus application-side filtering for teacher, student, or pairing audiences.
- `local/issues/004-recover-authoritative-state-after-reconnect.md`: Resume handling sends a fresh teacher or student snapshot for the reconnecting **Session ID** when authoritative state exists.
- `local/issues/004-recover-authoritative-state-after-reconnect.md`: Resume handling treats the newest socket for a **Session ID** as current and disconnects any older socket for that **Session ID**.
- `local/issues/004-recover-authoritative-state-after-reconnect.md`: Missed removed, activity-ended, pairing-ended, and chat state are represented as snapshot state rather than transient-only notifications.
- `local/issues/004-recover-authoritative-state-after-reconnect.md`: Chat messages are stored in in-memory pairing state before realtime delivery.
- `local/issues/004-recover-authoritative-state-after-reconnect.md`: Typing indicators remain ephemeral and are allowed to expire during disconnects.
- `local/issues/004-recover-authoritative-state-after-reconnect.md`: Tests cover reconnecting after a missed state change and receiving the current snapshot on the newest socket.
- `local/issues/004-recover-authoritative-state-after-reconnect.md`: The Socket.IO layer does not make socket IDs participant identity.
- `local/issues/005-add-realtime-boundary-regression-tests.md`: Tests cover explicit heartbeat configuration.
- `local/issues/005-add-realtime-boundary-regression-tests.md`: Tests cover one **Session ID** allowing only one live socket.
- `local/issues/005-add-realtime-boundary-regression-tests.md`: Tests cover a newer socket replacing an older socket for the same **Session ID**.

Changed:
- Renamed the existing connection registry replacement regression test to state the V1 boundary contract in repo domain language: an older **Realtime Connection** is replaced when the same **Session ID** resumes on a newer socket.
- Marked the matching acceptance criterion complete in `local/issues/005-add-realtime-boundary-regression-tests.md`.

Key decisions:
- Selected the topmost unchecked criterion: tests cover a newer socket replacing an older socket for the same **Session ID**.
- Kept the change at the test boundary because the production registry behavior was already implemented and covered.
- Preserved the existing assertions that the registration reports the replaced socket, the newer socket is current, and the older socket is no longer registered.

Blockers:
- None.

Files changed:
- `server/src/realtime/connectionRegistry.test.ts`
- `local/issues/005-add-realtime-boundary-regression-tests.md`
- `progress.txt`

Verification:
- Passed: `npm run test -w server`

Recommended next iteration:
- Continue `local/issues/005-add-realtime-boundary-regression-tests.md` with the next unchecked criterion: Tests cover current-socket disconnect behavior.
