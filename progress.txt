Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Added a workspace package named `@frempower/shared`.

Changed:
- Registered `shared` in the root npm workspace.
- Added `shared/package.json` for the private `@frempower/shared` ESM package.
- Added `shared/tsconfig.json` for strict TypeScript builds with declaration output.
- Added a minimal `shared/src/index.ts` export surface so the package can build now and receive contracts/constants/helpers in later iterations.
- Updated `package-lock.json` with the new workspace link.
- Marked the matching PRD task complete.

Key decisions:
- Kept this slice to package scaffolding only; product constants, acknowledgement types, snapshots, and normalization helpers remain separate unchecked tasks.
- Used the existing workspace build pattern and TypeScript dependency version.
- Did not add Vitest, Socket.IO wiring, authentication, database, Docker, or product flows.

Blockers:
- None.

Files changed:
- `package.json`
- `package-lock.json`
- `shared/package.json`
- `shared/tsconfig.json`
- `shared/src/index.ts`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm run build -w @frempower/shared`

Notes for next iteration:
- A good next task is to add shared product constants for Join Code length/range, default Character Names, chat message length, and disconnect timeout durations.
