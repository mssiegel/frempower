Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Shared package exports initial teacher/student snapshot and realtime payload types.

Changed:
- Added shared identity aliases for Join Code, Activity ID, Entity ID, Session ID, Character Name, Student Real Name, and Teacher Email.
- Added shared realtime event-name constants for initial teacher, student, and chat command/snapshot channels.
- Added teacher-facing snapshot types for lobby students, active pairings, completed chats, message previews, counts, and Peer Real Name Visibility.
- Added student-facing snapshot types that expose only the receiving student's state and allowed active pairing context.
- Added initial realtime command payload/result types and event-to-payload maps for future Client and Realtime Server wiring.
- Marked the matching PRD task complete.

Key decisions:
- Kept this iteration type-only so it establishes shared contracts without implementing host, join, pairing, chat UI, Socket.IO handlers, authentication, or persistence.
- Modeled `ActivityId` and `JoinCode` separately even though V1 uses the same five-digit value, because routes and join commands use those concepts differently.
- Used ISO timestamp strings for disconnect deadlines in snapshots so the server can remain authoritative while the Client renders display-only countdowns.
- Kept student snapshots audience-specific and intentionally omitted the Student List and unrelated Pairings.

Blockers:
- None.

Files changed:
- `shared/src/index.ts`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm run build -w @frempower/shared`

Notes for next iteration:
- A good next task is to export pure normalization helpers for Character Names and Student Real Names, because server validation and future Client duplicate-name cues both depend on the same trimming and case-normalization rules.
