Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Shared package exports pure normalization helpers for Character Names and Student Real Names.

Changed:
- Added shared helpers that trim Character Names and Student Real Names for storage while preserving casing and internal spacing.
- Added shared helpers that trim and lowercase Character Names and Student Real Names for duplicate comparison cues and authoritative validation.
- Marked the matching PRD task complete.

Key decisions:
- Kept normalization as plain pure functions in `@frempower/shared`, with no schema validation library.
- Split storage normalization from comparison normalization so saved names can preserve teacher/student casing while duplicate checks remain case-insensitive.
- Did not add Vitest in this iteration because that is a separate unchecked PRD task.

Blockers:
- None.

Files changed:
- `shared/src/index.ts`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm run build -w @frempower/shared`

Notes for next iteration:
- A good next task is to add Vitest for shared and server tests, because the new helpers and upcoming activity service behavior need executable coverage.
