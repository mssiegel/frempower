Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Command acknowledgement errors support stable machine-readable error codes separate from user-facing copy.

Changed:
- Added `COMMAND_ACKNOWLEDGEMENT_ERROR_CODES` and `CommandAcknowledgementErrorCode` exports to `@frempower/shared`.
- Added `CommandAcknowledgementErrorPayload` so acknowledgement errors carry a stable machine-readable `code`, separate user-facing `message`, and optional command-specific `details`.
- Tightened `CommandAcknowledgementError`, `CommandAcknowledgementResult`, and `commandAcknowledgementError` to use the structured error payload.
- Marked the matching PRD task complete.

Key decisions:
- Used a shared literal error-code vocabulary for common realtime command failures so client and server do not invent ad hoc strings.
- Kept user-facing copy in `message` rather than deriving it from the code, because different Client flows may need different copy for the same machine-readable failure.
- Kept optional `details` generic so future command payloads can attach structured context without widening the base contract.
- Did not add Vitest, Socket.IO wiring, authentication, database, Docker, or product flows.

Blockers:
- None.

Files changed:
- `shared/src/index.ts`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm run build -w @frempower/shared`

Notes for next iteration:
- A good next task is to export initial teacher/student snapshot and realtime payload types, because those contracts will shape the activity service and Socket.IO wiring.
