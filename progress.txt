Teacher host form setup flow progress

Current mode:
- AFK Ralph targets the local issue for the `/teacher` host form setup flow.
- Each Ralph iteration implements exactly one unchecked acceptance criterion from the selected local issue.
- `afk-ralph.sh` commits each successful iteration on the current named branch.
- Ralph must still update the local issue, `PRD.md`, and this file before it stops.

Current local issue:
- `local/issues/issue-4-001-build-teacher-host-form-setup-flow.md`

Completed local issues:
- None for GitHub parent issue #4 yet.

Completed criteria:
- None yet for `local/issues/issue-4-001-build-teacher-host-form-setup-flow.md`.

Changed:
- Retargeted Ralph planning from the completed Socket.IO backlog to the `/teacher` host form setup flow.
- Set `afk-ralph.sh` to default to `local/issues/issue-4-001-build-teacher-host-form-setup-flow.md`.
- Rewrote `PRD.md` around the teacher host form scope, out-of-scope boundaries, backlog, workflow, and verification.
- Reset this progress log for issue 4-001.

Key decisions:
- Keep issue 4-001 at the local client form boundary.
- Do not create a **Classroom Activity**, generate a **Join Code**, or wire host commands in this issue.
- Prefer focused client tests for validation and disabled/enabled **Host activity** behavior.
- Preserve repo domain language: **Teacher Page**, **Characters**, **Character Name**, **Character List**, **Teacher Email**, **Peer Real Name Visibility**, **Host activity**, and **Classroom Activity**.

Blockers:
- Parent issue #3 is listed as a blocker in the local issue. Ralph should inspect the current codebase before selecting a criterion; if the selected criterion is genuinely blocked by missing parent-issue work, record the blocker and stop without checking it off.

Files changed:
- `afk-ralph.sh`
- `PRD.md`
- `progress.txt`

Verification:
- Pending after this retargeting update.

Recommended next iteration:
- Start the topmost unchecked criterion in `local/issues/issue-4-001-build-teacher-host-form-setup-flow.md`: `/teacher` shows a desktop-only host form and a clear unsupported-width state on small screens.
