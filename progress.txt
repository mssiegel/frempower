Issue #3 progress

Current mode:
- Ralph remains human-in-the-loop.
- This iteration implemented exactly one unchecked PRD task and left the diff uncommitted for software engineering author review.

Completed:
- Create an in-memory activity service/store skeleton separate from Socket.IO handlers.

Changed:
- Added `server/src/activity/activityService.ts` with an in-memory activity service factory.
- Defined the initial `ClassroomActivityRecord`, service change, subscriber, and read API types.
- Kept the activity service free of Socket.IO imports and transport concepts.
- Created a server singleton activity service in `server/src/index.ts` without adding host, join, pairing, or chat flows.
- Added `@frempower/shared` as an explicit server workspace dependency for shared domain contract types.
- Marked the matching PRD task complete.

Key decisions:
- The skeleton currently exposes `subscribe`, `getActivity`, and `listActivities` only, leaving mutation commands for later focused tasks.
- Activity records are cloned on read so future domain state is not accidentally mutated by callers.
- Dependency injection for clock, random, and Join Code generation remains a separate unchecked task.

Blockers:
- None.

Files changed:
- `server/src/activity/activityService.ts`
- `server/src/index.ts`
- `server/package.json`
- `package-lock.json`
- `PRD.md`
- `progress.txt`

Verification:
- Passed: `npm test -w @frempower/server`

Notes for next iteration:
- A good next task is to make the activity service accept or centralize clock, random, and Join Code dependencies so deterministic Join Code collision retry tests can be added.
